{{- if eq .chezmoi.hostname "Designers-PC" -}}
return  {
    "supermaven-inc/supermaven-nvim",
    config = function()
      require("supermaven-nvim").setup({})
    end,
}
{{- else -}}
return {
  'github/copilot.vim',
}
{{- end -}}
